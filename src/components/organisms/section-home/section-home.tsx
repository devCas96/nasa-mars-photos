import styles from './section-home.module.css';

const SectionHome = () => (
  <section className={styles.home}>
    <h1>Let&apos;s start </h1>
    <p>Choose one rover to get latest photos from Mars API.</p>
  </section>
);

export default SectionHome;
